flask>=2.0.0

# 正版进服加密（AES/CFB8 + RSA，与 Minecraft Java 版一致）
# 不装则正版路径走 pycryptodome 兜底，可能不兼容
cryptography>=41.0.0

# 性能加速（可选，安装后自动启用）
uvloop>=0.19.0; sys_platform != 'win32'   # Linux/Mac 异步事件循环加速
pysimdjson>=5.0.0                          # SIMD JSON 解析加速（SLP 响应解析）
